package teste2.pergunta5;

public class Main {

/*    public double totaldesde(Data data, Categoria categoria) {
        double total = 0;

        for(Movimento m : getLinhasMovimentos()) {
            if (m.getDataValor().depois(data) && m.getCategoria().equals(categoria)) {
                total += getQuantiaMovimentada();
            }
        }

        return total;
    }*/
}
